<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <!-- //////////////////////////////////// css por defecto ///////////////////////////////////// -->
    <link rel="stylesheet" href="/gateway/file/MG/CSS/generic/jsxgraph" />
    <link rel="stylesheet" href="/gateway/file/MG/CSS/mathLive/mathlive-static" />
    <!--  ////////////////////////////// css general /////////////////////////////////////////// -->
    <% sources?.library?.forEach(item=> { %>
        <script defer src="/gateway/file/MG/LIB/<%=item%>"></script>
        <% }); %>
            <% sources?.style?.forEach(cssFile=> { %>
                <link rel="stylesheet" href="/gateway/file/MG/CSS/<%= cssFile %>">
                <% }); %>
                    <!-- ////////////////////////////////// librerias ////////////////////////////////////// -->

                    <!-- /////////////////////////////////// fin librerias ////////////////////////////////////// -->
</head>

<body>
    <style>
        :root {
            --keyboard-zindex: 10000;
            /* Asegúrate de que el valor sea mayor al de otros elementos */
        }
    </style>