<% sources?.engine?.forEach(item=> { %>
    <script defer src="/gateway/file/MG/ENG/<%= item %>"></script>
    <% }); %>
        <% sources?.generic?.forEach(item=> { %>
            <script src="/gateway/file/MG/GENERIC/<%= item %>"></script>
            <% }); %>
                <% sources?.validation?.forEach(item=> { %>
                    <script src="/gateway/file/MG/VAL/<%= item %>"></script>
                    <% }); %>

                        <script defer src="/gateway/file/MG/DEF/<%= sources?.definition %>"></script>
                        <script src="/gateway/file/MG/SEND/brains/send"></script>


                        </body>

                        </html>